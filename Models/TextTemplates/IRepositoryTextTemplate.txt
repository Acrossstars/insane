{{- for item in usings }}using {{ item }};{{- end }}

namespace {{ namespace }};

public interface I{{ aggregate_entity }}Repository : IRepository<{{ aggregate_entity }}>
{
    {{ for method in methods }}
    Task{{ method.type }} {{ method.name }}({{ for par in method.parameters }}{{ par.type }} {{ par.name }}{{ if ! for.last }}, {{ end }}{{- end }});{{ end }}
}